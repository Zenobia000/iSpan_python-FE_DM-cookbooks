# 複本與資料檔

上課主線是 `modules/module_01`–`11`。這裡是搬檔留下的複本與較大 CSV，不是排除箱。

| 資料夾 | 對應主線 |
|---|---|
| `video_training/` | Module 9 影片、Module 11 影片下游 |
| `lora_llm_training/` | Module 11 LoRA／SFT |
| `association_rules/` | Module 10；Instacart CSV 在 `datasets_instacart/` |
| `clustering/` | Module 10；Mall CSV 在 `datasets_mall_customers/` |

優先用各模組自己的 `notebooks/`。
